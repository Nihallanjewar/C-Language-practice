#include <stdio.h>
int main()
{
    int i = 0, n;
    do
    {
        printf("%d\n", i);
        i+=10;
    } while (i <= 100);
    return 0;
}

/*STEPS : Sabse pehle code run hoga fir condition check hogi. 
Code tab tak run hoga jab tak conditions satify nhi hoti 
as soon as condition satisfies the code stops and we get the output */ 
